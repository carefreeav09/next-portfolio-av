import React from "react";

const checkbox = () => {
  return (
    <div>
      <label htmlFor={`checkbox`}>
        <input type="checkbox" /> Label
      </label>
    </div>
  );
};

export default checkbox;
